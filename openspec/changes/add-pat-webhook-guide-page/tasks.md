## 1. Add the authenticated guide page

- [x] 1.1 Add a new authenticated UI route and handler method for the PAT token and webhook guide page.
- [x] 1.2 Create a server-rendered HTML template for the guide page using the existing shared layout and the approved PAT/webhook copy.

## 2. Expose the guide from the existing UI

- [x] 2.1 Add a primary navigation entry point so authenticated users can reach the guide page from anywhere in the app.
- [x] 2.2 Add at least one workflow-local CTA from the mirror setup experience, such as the create form or mirror detail page, so users can discover the guide when they need it.

## 3. Verify the guide behavior

- [x] 3.1 Add or update UI tests to confirm the guide page loads for authenticated users and includes the PAT and webhook instruction sections.
- [x] 3.2 Verify the shared layout still renders correctly with the new guide navigation on desktop and mobile-sized authenticated pages.
